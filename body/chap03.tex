
\chapter{面向海上雾天场景的小目标检测模型设计}
\label{chap:method}

本章以实时端到端检测器RT-DETR为基准框架，围绕海上雾天航拍小目标检测中普遍存在的特征弱化、语义混淆与端侧实时约束，对检测网络的主干结构与雾天鲁棒性机制进行优化设计，首先通过改进轻量化主干网络并引入多维注意力机制，提升微小目标特征的底层表达能力。其次构建基于状态空间模型的雾分布表征与特征增强分支，以实现雾天细节信息与环境先验的有效提取。随后设计动态物理引导特征融合机制，加强环境先验与深层语义信息的协同表达。最后提出分阶段协同训练策略，以保障模型在复杂环境下的稳定收敛与整体检测性能，构建兼顾实时性与鲁棒性的轻量级检测模型。


\section{端到端雾天目标检测算法整体框架}
\label{sec:overall_framework}


本文在实时目标检测模型 RT-DETR 的基础上构建了包含检测主干与物理先验辅助分支的协同结构，在特征层面引入了物理先验注入与语义融合。整体架构如图\ref{fig:overall_arch}所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=1.0\linewidth]{figures/模型整体架构图.png}
    \caption{面向海上雾天场景的小目标检测总体架构示意}
    \label{fig:overall_arch}
\end{figure}

网络主要由轻量化检测主干、加入雾分布表征模块的混合编码器以及解码器和检测头共同组成。针对微小目标特征弱化与常规主干网络计算资源消耗过大的问题，本文设计基于多维注意力机制的轻量级主干网络，将三维无参数注意力与倒残差自注意力引入特征提取阶段以增强微弱目标的底层语义表达能力。为解决常规网络将去雾与检测作为串行孤立任务导致的特征冲突问题，本文构建基于频域解耦与状态空间模型的雾分布表征与特征增强分支，并设计动态物理引导特征融合机制，将雾分布作为物理先验自适应注入检测特征流，实现环境先验与语义信息的协同表达。

从算法的整体数据流向来看，输入雾天图像$I_{fog}$首先进入检测主干，由轻量化残差网络逐层降维提取多尺度语义特征。在主干的特定中间特征层，网络分流构建雾分布表征与特征增强分支，该分支并不以生成视觉上绝对清晰的图像为终极目标，而是以提取雾气空间分布物理表征为核心任务，为后续特征的选择性增强提供依据。提取的物理先验与主干语义特征在颈部节点交汇，经动态融合模块后，送入高效混合编码器完成尺度内特征交互与跨尺度融合，最终由端到端解码器输出目标类别与边界框。在模型优化的训练策略层面，为保证雾分布表征具有物理意义并被检测网络有效利用，本文采用分阶段协同训练流程。训练阶段首先对雾分布表征与特征增强分支进行独立预训练，使其在清晰监督下形成稳定表征，避免联合训练初期因检测损失主导而产生无意义响应，随后进入联合微调阶段。为解决联合模型训练初期梯度震荡以及端到端一对一匹配导致的正样本不足问题，在检测微调阶段引入协作混合分配方案，通过并行辅助预测头提供更密集的监督信号。在最终的推理部署阶段，辅助检测头与重构监督支路被完全移除，仅保留主解码器的集合预测输出，从而在不增加任何额外推理计算成本的前提下，有效保障了联合模型的稳定收敛并大幅提升了雾天微小目标的检测召回率。
接下来将详细阐述每个模块的设计思路和实现细节。

\section{基于多维注意力机制的轻量化主干网络设计}
\label{sec:backbone}

海上无人机平台通常受限于机载算力、功耗与内存带宽等约束，检测模型在部署时需要在精度与实时性之间取得平衡。本文选用 RT-DETR 作为基准检测框架，该框架结合了 CNN 的局部特征提取能力与 Transformer 的全局建模能力，并通过轻量化编码器与 IoU-aware 优化策略实现了高效的端到端集合预测（其基础架构如图3-6所示）。RT-DETR由一个主干网络、一个高效混合编码器和一个带有辅助预测头的Transformer解码器组成。它将主干网络最后三个阶段的特征{S3,S4,S5}输入到编码器中。高效混合编码器通过尺度内特征交互(AIFI)和跨尺度特征融合(CCFF),将多尺度特征转换为一系列图像特征。随后,采用不确定性最小查询选择方法(IoU),选择固定数量的编码器特征作为解码器的初始对象查询。最后,带有辅助预测头的解码器迭代优化对象查询,以生成类别和边框。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{figures/RT-DETR模型架构图.png}
    \caption{RT-DETR模型架构图}\label{Fig2-3}
\end{figure}


然而，原始架构在处理雾天海面微小目标时，由于特征空间分布极不均匀，目标边缘与局部纹理往往在深层网络中发生不可逆的退化。为此，本文提出一种适用于有雾天气航拍小目标检测的改进主干网络。在语义特征提取阶段，本文选用参数规模更小的 ResNet-18 替代原始深层骨干，以显著降低前向推理延迟。ResNet-18 是一个较浅的网络，其参数量和计算量远小于 RT-DETR 中使用的 ResNet-50 。这使得它在推理时的速度更快，更适合对实时性要求高的任务。并且通常无人机搭载的计算单元在资源有限的设备上，ResNet-18由于计算量小，能够更好地适应这些资源受限的环境。虽然ResNet-18层数较少，但是在小目标检测任务中有时并不需要极其深层的特征提取网络。ResNet-18能够提供足够的特征抽象层级来满足此类任务的需求，在较小规模数据集上训练时也不容易过拟合。为弥补网络深度缩减带来的特征抽象能力下降，本文在残差模块中创新性地集成了无参数三维能量注意力机制（SimAM）与倒残差多头自注意力模块（iRMB）。前者基于神经科学统计理论抑制海面动态噪声并放大微弱高频特征；后者则在低计算复杂度下实现长距离全局交互，从而在维持轻量化的同时极大提升了网络对雾天小目标的特征保真与语义判别能力。


\subsection{无参数三维注意力模块}

为增强主干网络对微弱目标结构的选择性响应，本文在残差基本块内部引入无参数三维注意力模块（SimAM）。该模块为每个神经元定义了一个能量函数，有效地帮助模型学习小目标的特征，抑制非目标背景干扰，增强特征映射的表示能力，从而提高模型检测精度，弥补了RT-DETR算法在小目标处理上的不足。其结构如下图所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/SimAM结构图.png}
    \caption{三维注意力模块}\label{Fig2-3}
\end{figure}


无参数三维注意力模块是一个轻量级的注意模块，可以自适应地调整特征图中不同位置的重要性，并通过计算相似度得分和加权特征来提高目标检测的准确性。为了更好地实现注意力，找到重要神经元最简单的方法是测量神经元之间的线性可分性。在该模块中定义的能量函数，如式 \eqref{eq:simam_energy_3} 所示。无参数三维注意机制模块不需要在原始网络中添加参数，而是在一层中推断出特征图的三维注意权值。
设输入特征为单通道特征图 $X\in\mathbb{R}^{H\times W}$，其中目标神经元记为 $t$，其余神经元记为 $\{x_i\}_{i=1}^{M-1}$，$M=H\times W$ 表示该通道神经元总数。引入线性变换 $\hat{t}=w_t t+b_t$ 与 $\hat{x}_i=w_t x_i+b_t$，并构造如下能量函数：
\begin{equation}
e_t(w_t,b_t,y,x_i)=\bigl(y_t-\hat{t}\bigr)^2+\frac{1}{M-1}\sum_{i=1}^{M-1}\bigl(y_o-\hat{x}_i\bigr)^2 ,
\label{eq:simam_energy_3}
\end{equation}
式中，$y_t$ 与 $y_o$ 分别表示目标神经元与其余神经元的二值监督信号（在推导中取 $y_t=1$、$y_o=-1$），$w_t$ 与 $b_t$ 为线性变换的权重与偏差。最小化式 \eqref{eq:simam_energy_3} 等价于训练同一通道内神经元 $t$ 与其余神经元之间的线性可分性。由于同一通道内神经元可近似服从相同分布，均值与方差可预先计算以避免冗余运算，能量函数可进一步写为
\begin{equation}
e_t(w_t,b_t,y,x_i)=\frac{1}{M-1}\sum_{i=1}^{M-1}\bigl(-1-(w_t x_i+b_t)\bigr)^2+\bigl(1-(w_t t+b_t)\bigr)^2+\lambda w_t^2 ,
\label{eq:simam_energy_4}
\end{equation}
其中 $\lambda$ 为正则化系数。对式 \eqref{eq:simam_energy_4} 求解可得到线性变换参数的解析解。令
\begin{equation}
\mu_t=\frac{1}{M-1}\sum_{i=1}^{M-1}x_i ,
\label{eq:simam_mu_t}
\end{equation}
则权重 $w_t$ 与偏置 $b_t$ 的解分别为
\begin{equation}
w_t=-\frac{2(t-\mu_t)}{(t-\mu_t)^2+\frac{2}{M-1}\sum_{i=1}^{M-1}(x_i-\mu_t)^2+2\lambda},
\label{eq:simam_wt}
\end{equation}
\begin{equation}
b_t=-\frac{1}{2}(t+\mu_t)w_t .
\label{eq:simam_bt}
\end{equation}

进一步将解析解与通道统计量结合，可得到目标神经元对应的最小能量形式
\begin{equation}
e_t^\ast=\frac{4(\hat{\sigma}^2+\lambda)}{(t-\hat{\mu})^2+2\hat{\sigma}^2+2\lambda},
\label{eq:simam_et_star}
\end{equation}
其中 $\hat{\mu}$ 与 $\hat{\sigma}^2$ 分别表示该通道所有神经元的均值与方差：
\begin{equation}
\hat{\mu}=\frac{1}{M}\sum_{i=1}^{M}x_i ,
\label{eq:simam_mu_hat}
\end{equation}
\begin{equation}
\hat{\sigma}^2=\frac{1}{M}\sum_{i=1}^{M}(x_i-\hat{\mu})^2 .
\label{eq:simam_sigma_hat}
\end{equation}
由式 \eqref{eq:simam_et_star} 可知，$e_t^\ast$ 越小表示目标神经元与周围神经元差异越大，从而应赋予更高的重要性。于是以 $1/e_t^\ast$ 构造注意力权值，并通过 Sigmoid 函数避免权值过大，最终的输出可写为
\begin{equation}
X^\ast=\sigma\!\left(\frac{1}{E}\right)\otimes X ,
\label{eq:simam_output}
\end{equation}
其中 $E$ 对所有通道和空间维度的 $e_t^\ast$ 进行归一化处理，$\sigma(\cdot)$ 为 Sigmoid 函数，$\otimes$ 表示逐元素乘法。

在工程实现上，无参数三维注意力模块可直接以通道方差与逐元素平方偏差构造权重映射，本文将无参数三维注意力模块插入到 ResNet-18 基本块的第二个卷积层之后、与残差相加之前，使其在不破坏残差恒等映射的前提下对主分支特征进行细粒度重标定。该位置的选择旨在利用第二次卷积后更具判别性的局部结构响应完成抑噪与特征增强，从而提升雾天条件下小目标边缘与局部纹理在后续层级中的可见性。


\subsection{倒残差级联分组自注意力模块}
\label{subsec:irmb_cga}

仅依靠基于局部统计量的注意力重标定仍难以弥补浅层卷积网络在全局上下文关联方面的固有不足。雾天退化使局部纹理与边缘梯度被压缩，微小目标在局部窗口内的可判别信息显著减少，检测器在定位与分类时更依赖跨区域的上下文支撑。传统卷积受限于固定局部感受野，若仅通过加深网络或扩大卷积核以获取更大的接收域，将引入更高的参数量与计算量，从而与无人机端侧部署的轻量化约束相矛盾。基于这一矛盾，本文在主干网络的残差单元中引入倒残差级联分组自注意力模块，目标是在控制计算成本的前提下增强跨区域依赖建模能力，并提升雾天背景干扰下微小目标结构响应的可分辨性。

该模块的轻量化根基首先来自深度可分离卷积对卷积计算的解耦。标准卷积需要在空间维度与通道维度同时完成混合，为保证每个输出通道均与全部输入通道发生交互，卷积核的通道数必须与输入通道数一致，卷积核的个数与输出通道数一致，从而使计算量随输入输出通道数呈乘性增长。为量化对比，设输入特征为$f \in \mathbb{R}^{D_f \times D_f \times M}$、输出特征为$o \in \mathbb{R}^{D_o \times D_o \times N}$、卷积核大小为$D_k$，标准卷积的计算量近似为
\begin{equation}
\text{SConv} = D_k \cdot D_k \cdot M \cdot N \cdot D_f \cdot D_f,
\label{eq:sconv_flops_new}
\end{equation}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{figures/标准卷积.png}
    \caption{标准卷积示意图}
    \label{fig:dwconv_vs_sconv}
\end{figure} 

深度可分离卷积将空间卷积与通道混合拆分为两步：首先对每个输入通道独立执行逐通道空间卷积，再通过逐点卷积实现跨通道线性组合，因此其总计算量可近似为
\begin{equation}
\text{DWConv} = D_k \cdot D_k \cdot M \cdot D_f \cdot D_f + M \cdot N \cdot D_f \cdot D_f.
\label{eq:dwconv_flops_new}
\end{equation}
将式(\ref{eq:dwconv_flops_new})与式(\ref{eq:sconv_flops_new})相除得到二者计算量比值
\begin{equation}
K=\frac{\text{DWConv}}{\text{SConv}}=\frac{1}{N}+\frac{1}{D_k^2},
\label{eq:conv_ratio_new}
\end{equation}
由式(\ref{eq:conv_ratio_new})可见，在常见的$D_k=3$与较大的$N$设置下，深度可分离卷积能够稳定降低计算量。该优势在端侧算力预算内为更强的特征交互算子留出空间，从而使得在轻量化框架中引入长程依赖建模成为可能。图\ref{fig:dwconv_vs_sconv}给出了标准卷积与深度卷积的实现方式示意。
\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/深度可分离卷积.png}
    \caption{深度可分离卷积示意图}
    \label{fig:dwconv_vs_sconv}
\end{figure}

在深度可分离卷积的支撑下，本文采用倒残差结构作为模块的骨架，以进一步降低信息瓶颈并稳定深层训练。传统残差结构通过恒等映射缓解网络加深带来的梯度消失与性能退化，但当非线性激活作用于低维通道空间时，容易出现信息压缩与细粒度结构丢失。倒残差结构将先压缩后扩张的通道组织方式反转，先通过逐点卷积进行通道扩张，在高维空间内执行非线性变换与空间建模，最后再压缩回原始通道数并与恒等分支相加，从而在保持残差训练稳定性的同时降低低维激活造成的信息损失。倒残差结构如图\ref{fig:inverted_residual}所示，对输入特征为$X\in\mathbb{R}^{H\times W\times C}$，倒残差框架首先通过通道扩张映射将其升维到$\lambda C$通道，得到$X_e=\mathrm{MLP}_e(X)\in\mathbb{R}^{\lambda H\times W\times C}$,
其中$\lambda$为扩张因子。随后在升维空间内引入高效中间算子$\mathcal{F}(\cdot)$进行特征增强，得到$X_f=\mathcal{F}(X_e)\in\mathbb{R}^{\lambda H\times W\times C}$,最后通过通道压缩映射还原通道数得到$X_s=\mathrm{MLP}_s(X_f)\in\mathbb{R}^{ H\times W\times C}$，并与恒等分支相加输出得到$Y=X+X_s$,体现了倒残差框架对中间复杂算子计算规模的约束作用，即在保持输入输出维度一致的前提下，将主要计算集中于可控的升维空间，从而在端侧预算内实现更丰富的特征变换。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.4\linewidth]{figures/倒残差结构示意图.png}
    \caption{倒残差结构示意图}
    \label{fig:inverted_residual}
\end{figure}

在高效中间算子 $\mathcal{F}(\cdot)$ 的具体实例化中，标准的多头自注意力机制虽然能够同时学习多组关系建模结果，但在端侧推理中其全量计算会带来较高的矩阵乘法开销。同时在海上雾天场景下，大面积低频散射与背景扰动会使注意力分配趋于平滑，从而弱化对小目标局部高频结构的选择性聚焦。为兼顾全局交互有效性与端侧开销可控性，本文不采用标准多头自注意力作为中间算子，而引入级联分组注意力机制替代其全量注意力计算。两者的关键差异在于信息组织方式：标准多头自注意力将同一输入特征投影到多个子空间并行计算，但各注意力头之间相互独立，最终仅在输出端进行拼接与线性投影；级联分组注意力在构造查询、键、值之前沿通道维将特征显式分组，并通过级联递推将前序分组的输出注入后续分组，使后续分组在关注自身局部子空间的同时继承前序分组的上下文表达，从而在近似预算下提高信息容量并减少冗余计算。级联分组注意力的结构示意如图\ref{fig:cga_arch}所示。
\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/级联分组注意力模块框架图.png}
    \caption{级联分组注意力模块结构示意图}
    \label{fig:cga_arch}
\end{figure}

设升维特征为$X_e \in \mathbb{R}^{H \times W \times \lambda C}$，沿通道维将其划分为$G$个分组
\begin{equation}
X_e = \mathrm{Concat}\!\left(X_e^{(1)}, X_e^{(2)}, \ldots, X_e^{(G)}\right), \qquad X_e^{(g)} \in \mathbb{R}^{H \times W \times C_g},
\label{eq:cga_split_new}
\end{equation}
其中$\sum_{g=1}^{G}C_g=\lambda C$。为实现组间信息递进，引入级联交互输入$\tilde{X}_e^{(g)}$并定义递推关系
\begin{equation}
\tilde{X}_e^{(1)} = X_e^{(1)}, \qquad
\tilde{X}_e^{(g)} = X_e^{(g)} + \psi\!\left(\tilde{Z}^{(g-1)}\right), \ g \ge 2,
\label{eq:cga_cascade_in_new}
\end{equation}
其中$\tilde{Z}^{(g-1)}$为上一分组的注意力输出，$\psi(\cdot)$为用于维度对齐的线性映射。该递推关系确保第$g$组的注意力计算不仅依赖于自身通道子空间，还显式继承前序分组的上下文表达，从而缓解分组带来的信息割裂并提升特征多样性。在每个分组内部构造查询、键、值线性投影
\begin{equation}
Q^{(g)} = \tilde{X}_e^{(g)}W_Q^{(g)}, \quad
K^{(g)} = \tilde{X}_e^{(g)}W_K^{(g)}, \quad
V^{(g)} = \tilde{X}_e^{(g)}W_V^{(g)},
\label{eq:cga_qkv_new}
\end{equation}
并计算分组自注意力输出
\begin{equation}
Z^{(g)} = \mathrm{Softmax}\!\left(\frac{Q^{(g)}{K^{(g)}}^{\top}}{\sqrt{d_g}}\right)V^{(g)},
\label{eq:cga_attn_new}
\end{equation}
其中$d_g$为该分组的投影维度。最终将各分组输出在通道维拼接并线性映射得到聚合注意力特征
\begin{equation}
Z = \mathrm{Concat}\!\left(Z^{(1)},Z^{(2)},\ldots,Z^{(G)}\right)W_O.
\label{eq:cga_concat_new}
\end{equation}
相较于标准多头自注意力的并行独立头机制，上述级联递推使注意力表达在组间形成渐进式的信息整合，更适合在雾天背景干扰较强时保持对局部结构的选择性，同时避免全量注意力导致的过度平滑。

为兼顾长程交互与局部纹理建模的互补需求，本文将级联分组注意力与深度可分离卷积组合构成倒残差框架内的最终中间算子。记$\mathrm{DWConv}(\cdot)$为$3\times 3$深度可分离卷积，则
\begin{equation}
\mathcal{F}(X_e) = \mathrm{DWConv}(Z) + Z,
\label{eq:cga_dwconv_new}
\end{equation}
其中残差项用于稳定训练并保留注意力输出的主导结构响应。式(\ref{eq:cga_dwconv_new})的组合具有明确的动机：级联分组注意力用于建立跨位置依赖，缓解雾天小目标在深层抽象过程中被大面积海面背景淹没的问题；深度可分离卷积为注意力输出注入局部归纳偏置，使特征在边缘与方向性纹理处保持更高的空间选择性，从而抑制雾天低频散射干扰导致的特征过度平滑。

综上，本文提出的倒残差级联分组自注意力模块以深度可分离卷积降低计算成本，以倒残差升降维框架避免低维非线性带来的信息瓶颈，并以级联分组注意力替代标准多头自注意力以提高雾天场景下全局交互的有效性与选择性。该模块在不显著增加端侧推理负担的前提下强化了微小目标弱响应的上下文可分性，为后续改进残差单元的构建与雾天鲁棒检测性能提升提供了关键的结构支撑。如图\ref{fig:irmb_arch}展示了该模块的具体网络架构。
\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.4\linewidth]{figures/倒残差级联分组自注意力模块示意图.png}
    \caption{倒残差级联分组自注意力模块结构示意图}
    \label{fig:irmb_arch}
\end{figure}

\subsection{改进残差单元与主干网络}
本文选取ResNet-18作为轻量级主干网络的基础架构，并在其基本残差单元中有序集成上述无参数三维注意力机制与倒残差级联分组自注意力模块，以构建适用于海上雾天小目标检测的改进残差单元。ResNet-18由4个阶段的基本残差单元组成，每个阶段包含2个基本块，通道数分别为64、128、256、512，其结构如图\ref{fig:ResNet_18_arch}所示。
\begin{figure}[!htbp]
    \centering
    \includegraphics[width=1.0\linewidth]{figures/ResNet-18结构图.png}
    \caption{ResNet-18结构图}
    \label{fig:ResNet_18_arch}
\end{figure}

本文在每个阶段的第一个基本块中插入无参数三维注意力模块与倒残差级联分组自注意力模块，以增强浅层特征对微小目标的响应能力，同时保持后续层级的计算效率。
综合上述针对微小目标弱响应与复杂海面背景强干扰的核心增强机制，本文在标准轻量级残差网络的基础上完成了改进残差单元的构建。无人机嵌入式平台对推理时延与功耗具有严格约束，因此主干网络在设计上必须在特征表达能力与计算复杂度之间取得平衡。本文将无参数三维能量注意力机制与倒残差自注意力模块有序嵌入基本残差单元，旨在不显著增加计算量的前提下，增强浅层结构特征对微弱目标的可分辨性。改进后的整体基本单元结构如图\ref{fig:basicblock_arch}所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/改进后的basicblock.png}
    \caption{改进后的轻量化残差单元结构图}
    \label{fig:basicblock_arch}
\end{figure}

在卷积特征提取的初始阶段，网络引入无参数三维能量注意力机制对局部响应进行细粒度重标定与背景抑制。对于输入特征张量，该机制基于通道统计量构建能量函数，通过解析解形式为每个神经元生成显著性权重。设单通道特征序列为 $\{x_i\}_{i=1}^{N}$，其能量度量可表达为$E(x_i) = \frac{(x_i - \mu)^2}{\sigma^2 + \epsilon}$，其中 $\mu$ 与 $\sigma^2$ 分别为该通道特征响应的均值与方差，$\epsilon$ 为防止除零溢出的微小常数。该权重客观反映了神经元相对于背景统计分布的偏离程度，网络据此自适应放大目标区域的微弱高频响应并抑制海面波纹与反射等冗余背景噪声。

经过初步净化的特征随后被送入倒残差级联分组自注意力模块中进行轻量级的长距离空间交互建模，以补充全局上下文语义。该模块在倒残差结构的通道升降维框架内，嵌入具有动态建模能力的自注意力算子与深度可分离卷积，从而在较低计算成本下扩大有效感受野。最后通过通道压缩映射将特征降维回原始通道数，并与输入形成残差连接。这种混合架构统筹兼顾了卷积神经网络的局部高效性与自注意力机制的全局感知优越性。

改进残差单元的主分支特征处理过程遵循严密的时序逻辑递进。输入特征 $F_{in}$ 首先通过残差主分支的卷积堆叠提取局部结构响应；随后利用三维能量注意力机制进行显著性重标定；紧接着通过倒残差自注意力模块补充上下文语义；最后，该主分支的高阶语义输出与通过跳跃连接直接传递的恒等残差进行逐元素相加，并经过非线性激活函数生成该残差单元的最终输出 $F_{out}$。设标准卷积分支为 $\mathcal{C}(\cdot)$，三维能量注意力机制与倒残差自注意力模块分别记为 $\mathcal{S}(\cdot)$ 与 $\mathcal{R}(\cdot)$，$\delta(\cdot)$ 表示非线性激活函数，则改进残差单元的整体输出映射可表达为公式$F_{out} = \delta\left(F_{in} + \mathcal{R}\left(\mathcal{S}\left(\mathcal{C}(F_{in})\right)\right)\right)$,该组合拓扑结构保留了传统残差网络缓解梯度消失与保障收敛稳定性的优良特性，确保了微小目标特征在逐层前向传递过程中能够得到通道显著性与空间全局性的多维度联合强化。

改进后的轻量级主干网络将持续在三个不同的空间降采样尺度输出特征张量，分别记为集合 $\{C3, C4, C5\}$，供后续的混合编码器与端到端解码器使用。由于本文在整体架构中需要额外引入雾分布表征分支，并在相对高分辨率的 $C3$ 特征节点实施离散小波变换，主干网络浅层细粒度表达的物理真实性与数值稳定性尤为关键。该主干架构为后续的频域分离、雾分布特征提取以及多尺度全局交互提供了具备高保真度与强鲁棒性的输入基石。


\section{基于状态空间模型的雾分布表征与特征增强分支设计}
\label{sec:fog_branch}

第二章机理分析表明，雾天退化在成像层面主要由透射率衰减与空气光叠加共同导致，其宏观统计效应表现为低频成分相对增强而高频结构响应被压缩。对海上无人机航拍小目标而言，目标的判别信息高度依赖边缘梯度与细粒度纹理；在雾气干扰下，这些高频结构被显著削弱，导致检测主干在浅层到中层特征传递过程中更容易发生目标结构与海面背景扰动的混叠，最终表现为漏检与误检同步上升。为在保持端到端检测推理流程不变的前提下显式引入雾气空间分布相关的物理先验，本文在检测主干的中间尺度特征层构建雾分布表征分支，并将其输出作为物理引导信号与检测特征在特征层面完成紧耦合融合。该分支并不以生成视觉上最清晰的去雾图像为终极目标，而是以学习低频退化规律并输出具有物理可解释性的雾浓度注意图为核心目标，从而为后续小目标结构特征的选择性增强与抑制提供依据。分支整体流程如图\ref{fig:fog_branch}所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=1\linewidth]{figures/雾分布表征与特征增强分支流程图.png}
    \caption{雾分布表征与特征增强分支总体结构示意}
    \label{fig:fog_branch}
\end{figure}

在数据流上，检测主干在第$C3$尺度输出中间特征$F_3\in\mathbb{R}^{C_3\times H_3\times W_3}$后，首先通过二维离散小波变换（DWT）实现显式频域解耦，将特征分解为低频近似子带与三类方向性高频细节子带：
\begin{equation}
\mathrm{DWT}(F_3)\rightarrow \left\{F_3^{LL},F_3^{LH},F_3^{HL},F_3^{HH}\right\},
\label{eq:dwt_decompose}
\end{equation}
其中$F_3^{LL}\in\mathbb{R}^{C_3\times \frac{H_3}{2}\times \frac{W_3}{2}}$承载以透射率缓变与空气光抬升为主导的低频退化信息，而$\{F_3^{LH},F_3^{HL},F_3^{HH}\}$更集中表达边缘与纹理等高频结构。基于雾分布的低频主导特性，本文采用低频路径对$F_3^{LL}$进行状态空间建模以恢复退化相关的全局一致性表征，并在恢复过程中同步预测雾浓度注意图$T$；另一方面，高频路径对三类细节子带进行通道聚合与轻量增强，形成细节旁路特征以最大限度保留微小目标的边缘纹理。最终，低频路径输出的雾浓度注意图$T$作为物理先验，通过物理引导融合模块（PGM）对高频旁路与低频恢复特征进行调制，并将融合结果回注入RT-DETR的混合编码器（CCFF）以参与后续跨尺度融合与集合预测。
本文记低频输入为$F_{\mathrm{low}}=F_3^{LL}$，高频聚合为$F_{\mathrm{high}}=\mathrm{Concat}\!\left(F_3^{LH},F_3^{HL},F_3^{HH}\right),\qquad
\bar{F}_{\mathrm{high}}=\mathrm{Conv}_{1\times1}(F_{\mathrm{high}})$,其中$\bar{F}_{\mathrm{high}}$用于后续与物理先验的特征级融合。低频路径经Mamba恢复得到$F_{\mathrm{low}}'$，并通过雾浓度预测头输出雾浓度注意图$T$。在本文定义中，$T$为雾浓度图的归一化表示，其数值越大表示雾越浓、该区域细节越不可靠，数值越小表示雾较薄、结构信息相对可信。浓雾区域更需要引入低频恢复特征对结构进行补偿，同时对原始高频细节进行更强抑制，以避免将雾噪与海面反光伪纹理当作目标边缘传播到检测颈部。该分支的具体结构如下图所示\ref{fig:fog_branch_arch}。
\begin{figure}[!htbp]
    \centering
    \includegraphics[width=1\linewidth]{figures/雾分布表征与特征增强分支结构示意图.png}
    \caption{雾分布表征与特征增强分支结构示意图}
    \label{fig:fog_branch_arch}
\end{figure}

下文详细介绍各模块的设计细节与实现方式。

\subsection{Mamba Block模块设计}
\label{subsec:mamba_block}

低频路径的核心算子采用基于状态空间模型的Mamba结构，以线性复杂度建模长程依赖并获得对非均匀雾分布的全局表征能力。状态空间模型的基本思想是将序列建模转化为隐状态的递推更新过程。对于连续时间形式，可写为
\begin{equation}
\frac{\mathrm{d}h(t)}{\mathrm{d}t}=\mathbf{A}h(t)+\mathbf{B}x(t),\qquad y(t)=\mathbf{C}h(t),
\label{eq:ssm_cont}
\end{equation}
其中$h(t)$为隐状态，$x(t)$为输入，$y(t)$为输出，$\mathbf{A},\mathbf{B},\mathbf{C}$为系统参数。将二维特征按扫描顺序展开为序列$x=\{x_n\}_{n=1}^{L}$（$L=\frac{H_3}{2}\cdot\frac{W_3}{2}$），可得到离散状态更新形式
\begin{equation}
h_{n}=\bar{\mathbf{A}}h_{n-1}+\bar{\mathbf{B}}x_n,\qquad y_n=\mathbf{C}h_n,
\label{eq:ssm_disc}
\end{equation}
其中$\bar{\mathbf{A}},\bar{\mathbf{B}}$为离散化等效参数。与标准SSM不同，Mamba引入输入感知的选择性机制，使离散化相关量随输入内容动态变化，从而在雾分布突变区域增强记忆更新强度，在平坦区域抑制冗余传播，并将整体计算复杂度保持为关于$L$的线性规模，使其更适用于高分辨率特征的全局建模。
本文设计的Mamba Block由视觉状态空间块（Visual State Space Block, VSSB）与增强前馈网络（FFN）串联组成，并在两个子模块之间引入层归一化与残差连接以稳定训练。结构如图\ref{fig:mamba_block}所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.6\linewidth]{figures/Mamba块结构图.png}
    \caption{Mamba Block结构示意图}
    \label{fig:mamba_block}
\end{figure}

对输入特征$F_{\mathrm{low}}\in\mathbb{R}^{\frac{H_3}{2}\times\frac{W_3}{2}\times C}$，Mamba block的前向过程定义为
\begin{equation}
F_h=\mathrm{VSSB}\!\left(\mathrm{LN}(F_{\mathrm{low}})\right)+\alpha\cdot F_{\mathrm{low}},
\label{eq:mamba_res1}
\end{equation}
\begin{equation}
F_{\mathrm{low}}'=\mathrm{FFN}\!\left(\mathrm{LN}(F_h)\right)+\beta\cdot F_h,
\label{eq:mamba_res2}
\end{equation}
其中$\mathrm{LN}(\cdot)$为层归一化，$\alpha,\beta$为可学习的残差缩放因子，用于在训练早期抑制不稳定梯度并在收敛后逐步释放非线性变换能力。

与传统自注意力相比，视觉状态空间块的核心优势在于其全局建模能力具有线性复杂度，而雾分布在空间上通常是缓慢连续但又可能存在跨区域的浓度变化，这种变化对检测特征的影响是长程相关的，其在图像去雨、低光增强等低层视觉任务中表现出良好的长程建模能力；同时状态空间建模对低频缓变退化项具有更自然的表达能力，有利于从低频子带中提取与雾分布相关的全局一致先验。视觉状态空间块结构如下图\ref{fig:vssb}所示。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{figures/VSSB模块图.png}
    \caption{VSSB结构示意图}
    \label{fig:vssb}
\end{figure}

该模块包含两个并行分支，设输入特征为$X_{in}\in\mathbb{R}^{H\times W\times C}$，首先通过共享权重的线性映射将通道扩展至$\gamma C$（$\gamma$为通道扩展因子）。在第一分支中，扩展后的特征直接经SiLU激活以保留原始信息并提供门控基底；在第二分支中，特征先通过深度卷积提取空间级局部结构，再经SiLU增强非线性表征，随后输入二维选择性扫描模块（2D-SSM）建模长程空间依赖，并施加层归一化稳定分布。两分支输出通过哈达玛积融合以实现内容自适应的门控调制，最后通过线性层将通道压缩回$C$以保持与输入维度一致。此并行结构一方面利用门控分支保留低频退化项的稳定基线，另一方面通过深度卷积与二维选择性扫描模块捕获跨区域雾强变化与全局一致结构，从而更适配非均匀雾分布的空间连续性。

二维选择性扫描模块的结构如图\ref{fig:2d_ssm}所示，二维图像特征被展平成一个一维序列，沿四个不同的方向扫描：左上角到右下角，右下角到左下角，左下角到右上角和右上角到左下角。然后根据离散状态空间方程捕获每个序列的长期依赖关系。最后将四条路径的输出求和聚合，并重塑回二维特征图，从而同时捕获水平与垂直以及反向路径的长程依赖关系。该机制能够在保持线性复杂度的前提下获得接近全局感受野的建模能力。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=1.2\linewidth]{figures/2D-SSM结构图.jpg}
    \caption{2D-SSM结构示意图}
    \label{fig:2d_ssm}
\end{figure}

为进一步提升低频路径在复杂海面背景下的特征选择能力，本文在Mamba Block模块中的前馈网络采用增强注意力的门控前馈网络以调节信息流动，促进有效特征向深层传播并抑制相关性较低的特征。对输入$F_{in}\in\mathbb{R}^{H\times W\times C}$，FFN首先利用深度可分离卷积高效提取通道与空间维度的局部特征，随后通过非线性门控机制调节信息流动强度，并引入高效通道注意力（ECA）模块突出与退化分布相关的关键通道响应，最后通过$1\times1$卷积将通道压缩回$C$。其形式化表达为：$F_{out}=\mathrm{PConv}\!\left(\mathrm{ECA}\left(\delta_G\!\left(\mathrm{DSConv}(F_{in})\right)\right)\right)$,其中$\mathrm{PConv}(\cdot)$为$1\times1$卷积，$\mathrm{DSConv}(\cdot)$为深度可分离卷积，$\delta_G(\cdot)$为非线性门控算子，$\mathrm{ECA}(\cdot)$为高效通道注意力模块。该增强前馈网络在不显著增加计算开销的前提下增强通道选择性，使低频路径更聚焦于雾分布相关的有效表征。

雾天退化不仅在空间上呈现非均匀性，不同语义通道也对雾散射与空气光的敏感程度不同。部分通道可能更易对大面积低频背景产生响应，导致检测过程中出现误检；而与小目标轮廓相关的通道更需要被显式强化。因此，在增强前馈网络中引入通道注意力进一步补充 VSSB 的全局建模与门控机制在通道选择上的不足。高效通道注意力模块与 SE 模块不同，它不采用两层 MLP 的通道压缩与扩张，而是通过一个轻量的一维卷积在通道维度上建模局部跨通道依赖，从而避免显式降维带来的信息损失，并将额外参数与计算控制在极低水平。整体结构如图\ref{fig:eca} 所示，图中 C 为通道维度; k 为卷积核大小; GAP为全局平均池化。

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{figures/ECA结构图.png}
    \caption{ECA结构示意图}
    \label{fig:eca}
\end{figure}

高效通道注意力模块以标准卷积神经网络作为基本网络; 然后在卷积层输出后使用全局平均池化对特征图进行降维得到通道描述向量，再在通道维上进行 1D 卷积，最后通过 Sigmoid 得到通道权重并回乘到输入特征上学习不同通道的重要性。该模块通过对全局池化后的通道描述施加一维局部卷积实现跨通道交互，避免了全连接压缩带来的信息损失并保持较低开销。该过程有助于突出与退化分布相关的有效通道，提高后续雾浓度预测头的可读性与稳定性。


\subsection{Haar小波下采样模块}
\label{subsec:hwd}

低频路径在多层Mamba建模过程中需要通过下采样扩大有效感受野以形成更强的全局一致性表征，但常规的步幅卷积或池化容易造成空间信息不可逆丢失，尤其在雾天退化背景下，低频结构已被空气光抬升与对比度压缩干扰，进一步的下采样信息损失会削弱雾浓度空间分布的可辨性。为提升下采样阶段的信息保真度，本文采用Haar小波下采样（Haar Wavelet Downsampling, HWD）替代传统下采样操作。

Haar小波下采样模块首先对输入特征施加Haar小波变换，将其分解为四个子带（近似分量与三类方向分量），每个子带的空间分辨率为原输入的一半；随后将四个子带沿通道维拼接，通过$1\times1$卷积实现跨子带通道交互，再经归一化与激活稳定分布。设输入为$U\in\mathbb{R}^{C\times H\times W}$，则Haar小波下采样模块可形式化为$\{U^{LL},U^{LH},U^{HL},U^{HH}\}=\mathrm{Haar}(U), U^{*}=\mathrm{Concat}(U^{LL},U^{LH},U^{HL},U^{HH})$, $\mathrm{HWD}(U)=\phi\!\left(\mathrm{BN}\!\left(\mathrm{Conv}_{1\times1}(U^{*})\right)\right)$,其中$\mathrm{BN}(\cdot)$表示批量归一化，$\phi(\cdot)$为ReLU激活。相较于步幅卷积，Haar小波下采样在下采样的同时保留了更完整的频带信息，使低频路径在尺度压缩后仍能维持雾浓度的连续变化结构，为后续Mamba的长程聚合提供更稳定的输入分布。在整体结构中，Haar小波下采样模块被插入到低频U形路径的编码端，用于在多尺度层级上递进提取雾分布的全局一致性特征，并与解码端的上采样路径通过跳跃连接实现信息回流，从而在不显著增加计算开销的条件下提升雾浓度注意图$T$的空间分辨率一致性与边界稳定性。

\subsection{雾浓度预测头与去雾图像重构头}
\label{subsec:haze_head_low_restore}

低频路径在多层 Mamba Block 与Haar小波下采样/上采样的 U 形结构后得到低频恢复输出$F_{\mathrm{low}}'$，其同时承载雾天退化的低频散射分布与被压缩的结构信息。为使该表征能够以物理可解释的形式参与检测特征融合，本文在 $F_{\mathrm{low}}'$ 上设计雾浓度预测头与去雾图像重构头，两者共享同一低频主干，从而保证雾表征与恢复表征在同一隐空间内保持一致性。

在低频恢复输出$F_{\mathrm{low}}'$之上，本文采用雾浓度预测头生成雾浓度注意图$T$。考虑到雾浓度图的本质是单通道空间标量场，预测头采用$1\times1$卷积完成跨通道线性投影并压缩至单通道，再用Sigmoid将输出归一化到$[0,1]$区间：$T=\sigma\!\left(\mathrm{Conv}_{1\times1}(F_{\mathrm{low}}')\right)$,其中$T\in[0,1]^{1\times \frac{H_3}{2}\times \frac{W_3}{2}}$.$1\times1$卷积等价于对每个空间位置的通道向量进行线性判别投影，能够从高维低频恢复特征中抽取与退化强度最相关的方向；Sigmoid将其映射为可解释的浓度权重，便于后续作为门控信号参与特征融合。为避免仅通过去雾重建监督导致$T$退化为无意义常量，本文在预热阶段对$T$引入显式监督约束。对于合成雾数据，加雾过程可获得雾浓度先验标签$T_{gt}$，从而将预热损失定义为$\mathcal{L}_{warm}=\mathcal{L}_{rec}+\lambda_T \left\|T-T_{gt}\right\|_1$,其中$\mathcal{L}_{rec}$为重建一致性损失，$\lambda_T$为权重系数。

在低频恢复输出$F_{\mathrm{low}}'$之上，本文采用去雾图像重构头生成重构图$I_{\mathrm{rec}}$，用于在先验预热阶段提供成对清晰监督，从而约束低频表征学习到与大气散射退化一致的稳定映射。需要指出的是，该重构头的目的并非追求视觉意义上的最优去雾质量，而是以低计算开销提供稳定梯度，使低频路径输出的特征同时具备可重建性与可解释性。考虑端侧部署效率，该重构支路仅在预热阶段参与训练，检测协同阶段与推理阶段均移除。重构头采用一次上采样与卷积细化的解码结构将低频特征映射回图像空间，
并与清晰标签$I_{\mathrm{gt}}$计算像素级一致性损失$\mathcal{L}_{rec}=\|I_{\mathrm{rec}}-I_{\mathrm{gt}}\|_1$以强化低频恢复特征的物理一致性。



由于 $F_{\mathrm{low}}'$ 的空间分辨率为原图的 $1/2$，重构头采用一次 上采样与卷积细化的解码结构完成从特征域到图像域的映射。首先对 $F_{\mathrm{low}}'$ 进行局部整形与通道压缩，得到中间特征$U_0=\rho\!\left(\mathrm{Conv}_{3\times3}(F_{\mathrm{low}}')\right)$,$U_0\in\mathbb{R}^{C_r\times \frac{H}{2}\times \frac{W}{2}}$,其中 $C_r$ 为压缩后的通道数（如 $C_r=64$），$\rho(\cdot)$ 表示归一化与非线性组合，用于稳定特征分布。

随后采用可微上采样算子将特征恢复到原始分辨率，并用卷积进一步细化：
$U_1=\rho\!\left(\mathrm{Conv}_{3\times3}\big(\mathrm{Up}(U_0)\big)\right)$,$U_1\in\mathbb{R}^{C_r\times H\times W}$,其中 $\mathrm{Up}(\cdot)$ 为 双线性插值上采样。最后通过 $1\times1$ 卷积将特征投影到 RGB 空间，并输出重构去雾图像：$I_{\mathrm{rec}}=\sigma\!\left(\mathrm{Conv}_{1\times1}(U_1)\right)$,$I_{\mathrm{rec}}\in[0,1]^{3\times H\times W}$,
其中 $\sigma(\cdot)$ 为 Sigmoid 函数，用于将输出约束到归一化图像范围。

在预热阶段，重构头使用清晰图像标签 $I_{gt}$ 提供像素级一致性监督。为提升对局部高光反射与异常值的鲁棒性，本文采用 $L_1$ 损失作为主要监督项：$\mathcal{L}_{rec}=\left\|I_{\mathrm{rec}}-I_{gt}\right\|_{1}$.该损失鼓励低频路径学习到与雾退化相关的稳定映射关系，使得 $F_{\mathrm{low}}'$ 不仅能够提供可解释的雾浓度表征，还具备一定的重建能力，从而在后续的物理引导融合中更有效地补偿雾天退化对目标结构的干扰。

该策略使低频恢复与雾浓度预测形成同一特征干路的多任务一致性约束，使$F_{\mathrm{low}}'$既保留可用于重建的结构信息，又显式携带可分离的雾浓度表征，从而恢复与表征并行输出的目标。在推理阶段，重构支路被移除，仅保留雾浓度图$T$与低频恢复特征$F_{\mathrm{low}}'$作为物理先验与结构补偿输入参与PGM融合，从而避免额外计算开销并满足端侧实时约束。

推理阶段移除重构支路，仅保留雾浓度图 $T$ 与低频恢复特征用于 PGM 融合，从而避免额外计算开销并满足端侧实时约束。



\subsection{雾分布引导物理融合模块}
\label{subsec:pgm}

雾分布表征与特征增强分支的目标并非替代检测主干，而是以雾浓度注意图$T$为显式物理先验，引导检测特征在雾天退化区域实现选择性补偿，并在清晰区域避免不必要的过增强。为此，本文在低频恢复特征与高频特征分量之间设计物理引导融合模块（PGM），用于将低频路径输出的雾浓度先验注入高频细节旁路与低频恢复特征中，形成可直接供CCFF跨尺度融合使用的增强特征。该模块以雾浓度为门控信号，在浓雾区域降低对原始高频细节的依赖，并引入低频恢复特征进行结构补偿；在薄雾区域保留更可信的高频细节，从而缓解串行去雾检测中常见的伪影传播与特征冲突问题。

PGM的输入包括高频旁路特征$\bar{F}_{\mathrm{high}}\in\mathbb{R}^{C\times H_3\times W_3}$、低频恢复特征$F_{\mathrm{low}}'\in\mathbb{R}^{C\times \frac{H_3}{2}\times \frac{W_3}{2}}$以及雾浓度注意图$T\in[0,1]^{1\times \frac{H_3}{2}\times \frac{W_3}{2}}$。由于$T$与$\bar{F}_{\mathrm{high}}$空间分辨率不一致，首先进行可微尺度对齐：$T_s=\Psi(T;H_3,W_3),\qquad T_s\in[0,1]^{1\times H_3\times W_3}$,其中$\Psi(\cdot)$为双线性插值。基于本文对$T$的定义（雾越浓$T$越大），PGM采用反向门控的方式抑制浓雾区域的原始高频细节，并以残差形式保留恒等通路以避免误抑制带来的灾难性偏移：
\begin{equation}
F_{\mathrm{high}}^{g}=\bar{F}_{\mathrm{high}}+\alpha\left(\bar{F}_{\mathrm{high}}\odot (1-T_s)\right),
\label{eq:pgm_spatial_gate}
\end{equation}
其中$\alpha$为可学习缩放系数，$(1-T_s)$在雾薄区域取值更大，从而对可信细节给予更高保留与增益，而在浓雾区域自动减弱对原始高频的依赖，降低雾噪与反光伪边缘的传播风险。仅在空间维门控仍难以区分不同语义通道对退化的敏感性差异，因此PGM进一步引入通道重标定以抑制更易受雾干扰的冗余通道并突出与目标结构更相关的通道响应：
\begin{equation}
z=\mathrm{GAP}(F_{\mathrm{high}}^{g}),\qquad
w=\sigma\!\left(W_2\,\delta\!\left(W_1 z\right)\right),\qquad
\hat{F}_{\mathrm{high}}=w\odot F_{\mathrm{high}}^{g},
\label{eq:pgm_channel}
\end{equation}
其中$\mathrm{GAP}(\cdot)$为全局平均池化，$\delta(\cdot)$为非线性激活，$W_1,W_2$为可学习参数矩阵。

为将低频恢复特征显式注入到同尺度融合特征中，PGM将$F_{\mathrm{low}}'$上采样至$H_3\times W_3$并进行通道对齐得到$\hat{F}_{\mathrm{low}}$：
\begin{equation}
\hat{F}_{\mathrm{low}}=\mathrm{Conv}_{1\times1}\!\left(\mathrm{Up}(F_{\mathrm{low}}')\right),
\label{eq:pgm_low_up}
\end{equation}
其中$\mathrm{Up}(\cdot)$为双线性上采样。最终融合特征采用雾浓度引导的动态平衡机制：在浓雾区域（$T_s$大）更依赖低频先验的结构补偿，在薄雾区域（$T_s$小）更依赖保真的高频细节：
\begin{equation}
F_{S3}^{\mathrm{pgm}}=T_s\odot \hat{F}_{\mathrm{low}}+(1-T_s)\odot \hat{F}_{\mathrm{high}},
\label{eq:pgm_fuse}
\end{equation}
其中$F_{C3}^{\mathrm{pgm}}$作为增强后的$C3$尺度特征送入RT-DETR的CCFF模块参与跨尺度融合。该融合形式的优势在于将物理先验以软门控方式注入特征流，避免传统方法将去雾结果与检测特征简单拼接导致的尺度不一致与伪影放大；同时在浓雾区域通过低频先验提供结构性补偿，能够更稳定地支撑后续编码器与解码器对微小目标的定位与分类。

综上，本文构建的雾分布表征与特征增强分支通过 DWT 将低频退化与高频细节显式解耦，在低频路径利用 Mamba 的线性复杂度全局建模与 Harr小波下采样模块的无混叠下采样获得稳定的退化表征，并由轻量预测头输出雾浓度图；随后通过雾分布引导物理融合模块将低频路径输出的雾浓度先验注入高频细节旁路与低频恢复特征中，使增强特征在雾浓区域获得更强结构补偿、在清晰区域保持细节稳定，最终以特征级方式服务于 RT-DETR 的 CCFF 融合与端到端检测。

\section{分阶段协同训练策略设计}
\label{sec:training_strategy}

在本文提出的雾天小目标检测框架中，检测主干与雾分布表征与特征增强分支共同参与特征形成与判别映射，但两条路径的优化目标与监督信号存在显著差异。检测路径以类别判别与边界框回归为目标，其监督来自实例级标注且具有强任务驱动属性；雾分布表征分支的核心作用是学习雾气退化的空间分布规律并输出用于特征调制的先验掩膜 $T$，其表征质量依赖于对物理退化过程的稳定拟合。若在联合训练初期将两条路径直接强耦合在同一检测损失下，雾分布表征分支往往会受到幅值更大且更不稳定的检测梯度牵引，导致其输出的透射率/雾强注意图趋于退化为近似常数分布或噪声纹理，从而削弱物理引导融合（PGM）对雾天退化区域的选择性补偿作用。

另一方面，端到端集合预测检测器在训练阶段依赖一对一匹配监督，该监督天然稀疏：每张图像仅有有限数量的预测查询会被分配为正样本。对海上航拍小目标密集场景而言，稀疏监督使训练早期的有效梯度更为不足，易出现解码器收敛缓慢与定位漂移，进而加剧联合优化过程中的梯度震荡。基于上述多任务联合优化的梯度冲突与稀疏监督问题，本文将训练过程划分为两个连续阶段：先验预热阶段与检测协同阶段。先验预热阶段的目标是使雾分布表征分支在特征域内学习到与雾天退化规律一致的映射关系，使其输出的雾分布注意图 $T$ 具有稳定的空间可解释性与数值分布；检测协同阶段在加载预热权重的基础上，将 $T$ 用于高低频调制融合，并在冻结先验分支的条件下优化检测主干、混合编码器与解码器，使检测器学习到如何使用先验。

为便于描述，记雾分布表征与特征增强分支参数为 $\Theta_{\text{fog}}$，其预热阶段重构头参数为 $\Theta_{\text{rec}}$；检测相关参数为 $\Theta_{\text{det}}$（。整体训练目标可概括为：
\begin{equation}
\textbf{Stage-1:}\quad \min_{\Theta_{\text{fog}},\Theta_{\text{rec}}}\ \mathcal{L}_{rec},
\qquad
\textbf{Stage-2:}\quad \min_{\Theta_{\text{det}}}\ \mathcal{L}_{det},
\label{eq:two_stage_objective}
\end{equation}
其中 Stage-2 中对 $\Theta_{\text{fog}}$ 施加冻结约束，确保先验稳定。


先验预热阶段仅激活雾分布表征分支与重构监督支路，用成对清晰监督约束其学习稳定的退化相关表征。该阶段输入为有雾图像 $I_{fog}$，雾分布表征与特征增强分支输出低频先验特征并通过重构头生成重构图像 $I_{rec}$。为避免过度强调高频纹理导致分支学习偏向视觉增强而偏离低频退化规律，本文采用鲁棒的像素级一致性损失作为主要监督：$\mathcal{L}_{rec} = \left\|I_{rec}-I_{gt}\right\|_{1}$,其中 $I_{gt}$ 为对应清晰图像。该损失对局部异常值具有更强鲁棒性，有利于抑制雾天场景中高光反射等局部极值对梯度的主导作用，使网络更稳定地拟合全局低频退化分布。

需要强调的是，预热阶段的重构监督仅用于约束雾分布表征分支的特征隐空间，使其输出的雾分布注意图 $T$ 具有可解释的空间分布。因此，预热阶段不接入检测解码器与检测损失，避免检测梯度在先验尚未稳定时干扰其学习过程。该阶段训练完成后，保存 $\Theta_{\text{fog}}$ 的权重作为后续检测协同阶段的初始化先验。

检测协同阶段加载预热得到的雾分布表征分支权重，并正式接入物理引导融合模块以及完整的端到端检测链路。为避免先验表征在强检测梯度下发生语义漂移，本阶段对雾分布表征分支参数实施冻结与梯度拦截：$\nabla_{\Theta_{\text{fog}}}\mathcal{L}_{det}=0,\Theta_{\text{fog}} \leftarrow \Theta_{\text{fog}}$,同时，预热阶段的重构头 $\Theta_{\text{rec}}$ 在本阶段被移除或停用，仅保留雾分布注意图 $T$ 用于特征调制。这样，检测损失的梯度将主要用于更新 $\Theta_{\text{det}}$，使模型学习在不同雾强区域下如何通过 $T$ 对高频结构特征进行有效补偿与抑制，从而提升雾天条件下的检出稳定性。

在检测任务的损失构造上，本文沿用端到端集合预测范式。检测总损失由分类损失与边界框回归损失构成，并在匈牙利匹配约束下实现预测集合与真实目标集合的一一对应：
\begin{equation}
\mathcal{L}_{det} = \mathcal{L}_{cls} + \lambda_{1}\mathcal{L}_{L1} + \lambda_{2}\mathcal{L}_{giou},
\label{eq:det_loss}
\end{equation}
其中 $\lambda_1,\lambda_2$ 为权重系数。该集合预测范式避免了 NMS 等后处理对推理时延的引入，符合端侧实时部署目标。

端到端检测器的一对一匹配监督在小目标密集场景下容易导致正样本数量不足，训练早期解码器获取的有效梯度较弱，从而造成收敛缓慢。为提高监督密度，本文在检测协同阶段引入协作混合分配思想：在训练期为编码器输出附加多个并行辅助检测头，采用一对多的密集分配策略提供更丰富的正负样本对；推理期移除所有辅助头，仅保留主解码器输出，从而不增加部署时计算开销。

设编码器输出特征为 $F_{enc}$，并行辅助预测头集合为 $\{\mathcal{H}_k\}_{k=1}^{K}$，则第 $k$ 个辅助头输出为
\begin{equation}
\hat{Y}_k = \mathcal{H}_k(F_{enc}),
\end{equation}
对应的辅助损失为 $\mathcal{L}_k$。训练阶段总损失可写为
\begin{equation}
\mathcal{L} = \mathcal{L}_{det} + \sum_{k=1}^{K}\gamma_k \mathcal{L}_k,
\label{eq:total_hybrid_loss}
\end{equation}
其中 $\gamma_k$ 为辅助损失权重。该结构通过密集监督增强编码器特征的判别性，并为主解码器提供更充分的训练信号，有助于改善雾天条件下小目标的定位稳定性与召回率表现。推理阶段去除辅助头后，模型仍保持端到端集合预测的高效推理流程。

综合上述设计，本文训练流程遵循先预热后协同的两阶段范式。预热阶段以重构监督约束雾分布表征与特征增强分支学习稳定的低频先验映射，检测协同阶段冻结雾分布表征与特征增强分支并优化检测路径与融合模块，辅以辅助检测头增强监督密度。该策略保证了训练阶段引入的额外机制不会在部署时带来额外计算负担，同时确保先验表征在训练全程保持稳定有效，从而使物理引导融合在雾天场景下产生可量化的检测性能增益。本节提出的分阶段协同训练策略为后续实验章节的对比与消融提供可操作的训练范式，同时也为复杂结构在小样本雾天数据条件下的稳定收敛提供工程可行性保证。

\section{本章小结}
\label{sec:chap3_summary}

本章围绕海上雾天无人机航拍小目标在雾散射退化下易出现结构信息弱化与目标背景混淆的问题，基于实时端到端检测框架 RT-DETR，构建了一种兼顾鲁棒性与端侧实时性的雾天小目标检测模型，并对其关键模块与训练策略进行了系统阐述。

在结构设计方面，本章首先以轻量化为约束对检测主干进行改造：以 ResNet-18 作为基础骨干以降低推理开销，并在残差单元中引入无参数三维注意力与倒残差级联分组自注意力模块，在不显著增加计算量的前提下增强浅中层对微小目标弱响应的表征能力与跨区域上下文关联能力。其次，为显式建模雾的空间非均匀分布并形成可利用的物理先验，本章在 $C3$ 尺度构建雾分布表征与特征增强分支：通过 DWT 将低频退化与高频结构解耦，在低频路径利用基于状态空间模型的 Mamba 进行长程依赖聚合，并结合 Haar 小波下采样在多尺度上维持退化表征的稳定性；在该低频表征上进一步引入轻量预测头输出雾浓度注意图，用于表征雾相关退化强度的空间分布。随后，本章设计雾分布引导的物理融合模块，在浓雾区域降低对原始高频细节的依赖并引入低频恢复特征进行结构补偿，在薄雾区域尽可能保留可信高频细节，从而将物理先验以软门控方式注入 RT-DETR 的 CCFF 跨尺度融合链路。

在训练策略方面，本章提出分阶段协同训练流程以缓解多任务梯度冲突与端到端集合预测监督稀疏带来的收敛不稳定：先验预热阶段仅优化雾分布表征与特征增强分支及其重构监督支路，使低频表征学习到与散射退化一致的稳定映射并提升雾浓度注意图的可解释性；检测协同阶段冻结先验分支，仅优化检测主干、PGM 与编码解码器，使模型学习如何在不同雾强区域有效利用先验，同时在训练期引入协作混合分配的辅助监督以提高正样本密度，推理期移除辅助头与重构支路以确保部署开销可控。

综上，本章完成了从轻量主干增强、雾分布先验建模到物理引导融合与分阶段训练的完整方法，为下一章的对比实验、消融实验与可视化分析提供了明确的结构依据与完整的方法基础。